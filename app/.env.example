# Copy to `app/.env` and fill in. The real file is gitignored.
#
# The Gemini API key stays on the Dart side and is injected by the
# shelf proxy (`lib/src/shell/gemini_proxy.dart`) into each outgoing
# Google Gemini request. The Unity WebGL bundle never sees this key.
GEMINI_API_KEY=
